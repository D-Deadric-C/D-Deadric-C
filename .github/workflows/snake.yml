name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Generate contribution snake
        uses: Platane/snk@v3
        with:
          github_user_name: D-Deadric-C
          # Pure monochrome, inverted between themes.
          # color_dots runs empty cell -> busiest day, so the ramp ends on the
          # "ink" colour: white on dark, black on light.
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#000000&color_dots=#FFFFFF,#CCCCCC,#999999,#4D4D4D,#000000
            dist/github-contribution-grid-snake-dark.svg?color_snake=#FFFFFF&color_dots=#000000,#333333,#666666,#B3B3B3,#FFFFFF

      - name: Push snake animation to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
